{/*import React, { useState} from "react"

export default function Topright() {
  const [show, setShow] = useState(true);

  const handleClick = (event) => {
    setShow(!show);
  };

  return (
    <div id="Topright">
      <button onClick={handleClick}>
        {show ? "Hide Text" : "Show Text"}
      </button>
      {show ? <h3>Hola</h3> : <h3>Adiós</h3>}
    </div>
  )
}*/}